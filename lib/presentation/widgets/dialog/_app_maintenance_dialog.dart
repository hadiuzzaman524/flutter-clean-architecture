part of 'dialog.dart';

class AppMaintenanceDialog extends StatelessWidget {
  const AppMaintenanceDialog({
    super.key,
    required this.title,
    required this.description,
  });

  final String title;
  final String description;

  @override
  Widget build(BuildContext context) {
    return Placeholder();
  }
}
